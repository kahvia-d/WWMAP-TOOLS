# 2026.9.9.7 程序更新热修复

修复程序自更新后的首次试运行会在签名程序目录生成 `imgui.ini` 的问题。该运行时布局缓存此前会被下一次启动的完整性检查误判为清单之外的文件，从而回退到原程序版本。

- ImGui 不再在程序目录自动读写布局缓存；完整程序包在试运行和后续重启时保持签名文件清单不变。
- 不迁移、不删除或覆盖 `%LOCALAPPDATA%\IMao-WinUI` 中的点位完成记录、路线、筛选和个人设置。
- 本次为程序热修复，继续使用 `map-baseline-1` 及既有已验证的地图资源。

构建、签名验包、公开下载校验和稳定更新通道推进以实际发布报告为准。
